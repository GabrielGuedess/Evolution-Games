import { Story, Meta } from '@storybook/react/types-6-0';

import { ScrollDown } from './ScrollDown';

export default {
  title: 'Atoms/ScrollDown',
  component: ScrollDown,
} as Meta;

export const Default: Story = () => <ScrollDown />;
